//仅用于记录开发过程中的构思
# 1. 页面布局
# 2. 页面功能
https://ant-design.antgroup.com/components/calendar-cn
https://procomponents.ant.design/components/editable-table
# 3. 页面交互
# 4. 页面数据
首先确定数据库中所需要存放的数据类型
计划表：
计划拥有者（用户ID）、计划名称、计划内容、计划创建时间、计划状态、计划完成时间

用户表：
用户ID、用户名、账号、密码、邮箱、用户类型、用户标语


# 5. 页面性能优化

以上内容已过期，现在有一套新的数据体系未记录。

已知bug：
(已修复)计划表中的当前任务能够实时修改了，但是新建计划时无法刷新新建的任务内容，该bug在连接数据库后进行修复(已修复)
在对subitem进行操作新建任务的时候，标记完成状态为已解决或未解决时，在calendar中的状态显示都为绿点（如未解决，标记颜色应为黄点）(已修复)

（已修复）对计划进行新建后，在新建计划表中进行subitem操作时，保存后新建计划会消失
（bug已定位：studyplanlist.tsx的18~30行，index.tsx的95~99行
 bug预计原因：未能成功及时更新handlemainplanitemChange，导致保存时渲染会导致默认数组重新赋值
 预计解决计划：在新建计划的即刻进行保存mainplan）
以下为未修复




未来优化内容：
less和tsx的分离，当前大部分样式还嵌入在tsx文件中，后续应尝试使用ai进行分离至less文件。


未完成内容：
StudyPlan的index.tsx文件中的内容仅做了大致框架的搭建，并未实际填充内容

已完成内容：
对index.tsx的studyplan请求进行优化(现已更改为Planlist)，mainplans要根据username来进行查询。
新建了usestate为data，用于进行第一次查询。
查询格式变更为
username: 123, mainplans:[1,2,3]
以上mainplans的123分别为mainplans每一项的planid 1 2 3
根据username获取一个储存planid的列表mainplans然后再次返回后端根据planid进行mainplan的二次查询


下一步内容：
针对index.tsx中当前的两个报错进行相关内容的修改