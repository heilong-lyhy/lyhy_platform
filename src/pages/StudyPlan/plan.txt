//仅用于记录开发过程中的构思
# 1. 页面布局
# 2. 页面功能
https://ant-design.antgroup.com/components/calendar-cn
https://procomponents.ant.design/components/editable-table
# 3. 页面交互
# 4. 页面数据
首先确定数据库中所需要存放的数据类型
计划表：
计划拥有者（用户ID）、计划名称、计划内容、计划创建时间、计划状态、计划完成时间

用户表：
用户ID、用户名、账号、密码、邮箱、用户类型、用户标语


# 5. 页面性能优化

以上内容已过期，现在有一套新的数据体系未记录。

已知bug：
(已修复)计划表中的当前任务能够实时修改了，但是新建计划时无法刷新新建的任务内容，该bug在连接数据库后进行修复(已修复)
以下为未修复
在对subitem进行操作新建任务的时候，标记完成状态为已解决或未解决时，在calendar中的状态显示都为绿点（如未解决，标记颜色应为黄点）


未来优化内容：
less和tsx的分离，当前大部分样式还嵌入在tsx文件中，后续应尝试使用ai进行分离至less文件。


未完成内容：
StudyPlan的index.tsx文件中的内容仅做了大致框架的搭建，并未实际填充内容
